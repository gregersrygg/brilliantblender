{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "grep -q '\"command\": *\"[^\"]*git commit' || exit 0; cd \"$CLAUDE_PROJECT_DIR\" && npx playwright test",
            "timeout": 300000,
            "statusMessage": "Running Playwright tests before commit..."
          }
        ]
      }
    ]
  }
}
